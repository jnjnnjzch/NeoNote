name: Eraser-priority source patch

on:
  push:
    branches:
      - agent/product-quality-loop
    paths:
      - scripts/eraser_priority_patch.py
      - .github/workflows/product-quality-patch.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout quality branch
        uses: actions/checkout@v4
        with:
          ref: agent/product-quality-loop
          fetch-depth: 0

      - name: Give the physical eraser priority
        run: python3 scripts/eraser_priority_patch.py

      - name: Validate patch whitespace
        run: git diff --check

      - name: Commit patched sources
        shell: bash
        run: |
          if git diff --quiet; then
            echo 'Eraser-priority patch already applied.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/src/main/java
          git commit -m 'Prioritize the physical S Pen eraser'
          git push origin HEAD:agent/product-quality-loop
