name: Apply navigation tap correction

on:
  push:
    branches:
      - agent/product-quality-loop
    paths:
      - scripts/navigation_tap_patch.py
      - .github/workflows/navigation-tap-patch.yml

permissions:
  contents: write

jobs:
  patch-and-clean:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout quality branch
        uses: actions/checkout@v4
        with:
          ref: agent/product-quality-loop
          fetch-depth: 0

      - name: Apply navigation tap correction
        run: python3 scripts/navigation_tap_patch.py

      - name: Remove temporary patch files
        run: |
          git rm -f scripts/navigation_tap_patch.py .github/workflows/navigation-tap-patch.yml

      - name: Commit corrected clean tree
        shell: bash
        run: |
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/src/main/java
          git commit -m 'Keep finger taps passive in navigation tools'
          git push origin HEAD:agent/product-quality-loop
